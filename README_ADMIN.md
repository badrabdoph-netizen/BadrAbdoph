# Admin panel (Password login)

Open `/admin` and enter the password.

## Required Railway Variables
- `DATABASE_URL`
- `JWT_SECRET`
- `ADMIN_PASSWORD`

## Notes
- This setup does **not** require OAuth.
- Content changes are saved to the database.
